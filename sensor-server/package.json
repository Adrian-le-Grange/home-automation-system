{
  "name": "sensor-server",
  "version": "1.0.0",
  "description": "Server that provides an API interface for controlling and monitoring devices.",
  "main": "sensorServer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adrian le Grange",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
